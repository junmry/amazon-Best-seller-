# OpenClaw 工作流优化方案
# 目标：减少 token 消耗，提升观察质量，消灭重复劳动

---

## 一、任务合并（6个cron → 3个）

### 合并 A：早盘信息包（9:47）
原任务：家具日报 + 总统口播
合并逻辑：都是信息收集型，共享网络搜索 API
输出格式：
```
🪑 家具：3个趋势词 + 1个异常（如有）
🏛️ 总统：今日核心主题 + 话术变化（如有）
```
正常日 ≤ 200 token，异常日才展开

### 合并 B：加密市场包（11:47）
原任务：大户异动监控 + HYPE技术分析
合并逻辑：共享 Hyperliquid API 数据拉取
流程：
1. 拉取一次 51 币种数据
2. 跑异动评分（资金费率/成交量/波动率）
3. 跑技术指标（RSI/MACD/布林带）
4. 输出：异动摘要 + 技术趋势摘要
正常日 ≤ 300 token，只有 🔴 评分才展开

### 独立 C：运势提醒（17:30）
保留独立，个性化内容无法合并

---

## 二、输出格式：三级触发

| 级别 | 条件 | 输出长度 | 示例 |
|------|------|---------|------|
| L1 静默 | 无异常，无变化 | 不发或 3 行 | "今日平静，无异动" |
| L2 摘要 | 有异常但非极端 | 10-15 行 | 标记异常币种 + 1句判断 |
| L3 详报 | 极端信号或连续模式 | 完整分析 | 展开技术 + 跨日对比 + 建议 |

触发规则：
- 大户监控：仅当 🔴≥6 分或 🟠≥3 个币种时触发 L3
- 家具日报：仅当检测到"新趋势词"或"已追踪趋势反转"时触发 L3
- 总统口播：仅当检测到话术变化或重大政策转向时触发 L3

---

## 三、观察层独立：周报模式

每日任务只记录**原始标记**（如：SEI Z-score -2.6）到 memory/YYYY-MM-DD.md

每周日（或用户要求时）做一次**跨日聚合**：
- 读取最近 7 天 memory 文件
- 提取连续模式（如：某币种连续 3 天资金费率恶化）
- 生成「本周观察」摘要

这样每日 token 不膨胀，观察质量不下降。

---

## 四、脚本级优化

### 1. API 共享缓存
```python
# 在 /tmp/ 下缓存当天 API 数据
hl_cache_file = f"/tmp/hl_data_{datetime.now().strftime('%Y%m%d')}.json"
if os.path.exists(hl_cache_file):
    data = json.load(open(hl_cache_file))
else:
    data = fetch_hyperliquid_api()
    json.dump(data, open(hl_cache_file, 'w'))
```
大户监控 + 技术分析共享同一缓存，减少 50% API 调用

### 2. 报告文件压缩淘汰
```bash
# 添加到每日任务脚本末尾
find memory/crypto-scans/ -name "*.md" -mtime +7 -exec gzip {} \;
find memory/furniture-trends/ -name "*.md" -mtime +7 -exec gzip {} \;
```

### 3. 记忆文件 TTL
```bash
# 每月 1 号 3:00 cron
find memory/ -name "*.md" -mtime +30 -not -name "MEMORY.md" -delete
```

---

## 五、执行清单

- [ ] 删除原 HYPE 技术分析 cron（10:40）
- [ ] 修改大户异动监控脚本：加入技术指标计算，输出格式改为三级触发
- [ ] 修改家具日报脚本：加入总统口播内容，输出格式改为三级触发
- [ ] 添加 API 缓存逻辑到 hl_perp_monitor.py
- [ ] 添加报告文件压缩逻辑到各脚本
- [ ] 添加 memory/ 目录清理 cron（每月 1 号）
- [ ] 测试一周，观察 token 消耗和输出质量

---

## 六、预期效果

| 指标 | 当前 | 优化后 |
|------|------|--------|
| 每日 cron 任务数 | 6 | 3 |
| 每日 API 调用（加密） | 2次（异动+技术） | 1次（共享缓存） |
| 正常日输出 token | 2000-4000 | 500-800 |
| 异常日输出 token | 4000-8000 | 不变（值得展开） |
| memory/ 目录增长 | 每日 +1 文件 | 每日 +1 文件，30天后清理 |
| 观察质量 | 单日快照 | 跨日模式 + 周日聚合 |
